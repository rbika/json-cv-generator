import { Container } from '@/components/container'

const HomePage = () => {
  return (
    <Container className="mt-5">
      <p className="">Professional CVs from JSON files.</p>
    </Container>
  )
}

export default HomePage
